FOOTBALL_DATA_API_TOKEN=replace_with_your_token
SPORTMONKS_API_TOKEN=replace_with_your_token

# Optional deployment database. When this is set, the ESPN scraper writes live
# match data to PostgreSQL and the FastAPI app reads live state from there.
DATABASE_URL=postgresql://user:password@host:5432/database
